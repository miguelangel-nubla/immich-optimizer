tasks:
- name: jpeg-xl
  command: cjxl --lossless_jpeg=0 -q 75 {{.src_folder}}/{{.name}}.{{.extension}} {{.dst_folder}}/{{.name}}.jxl
  extensions:
  - jpeg
  - jpg
  - png
  - pgx
  - pam
  - pnm
  - pgm
  - ppm
  - pfm
  - gif
  - exr

- name: heic2jxl
  command: ./heic2jxl.sh {{.src_folder}} {{.name}} {{.extension}} {{.dst_folder}}
  extensions:
  - heic
  - heif

- name: caesium
  command: caesiumclt --keep-dates --exif --quality=85 --output={{.src_folder}} {{.dst_folder}}/{{.name}}.{{.extension}}
  extensions:
  - jpeg
  - jpg
  - png
  - tiff
  - tif
  - webp
  - gif

- name: vips
  command: vips copy {{.src_folder}}/{{.name}}.{{.extension}} {{.dst_folder}}/{{.name}}.jxl[Q=75]
  extensions:
  - dng

- name: handbrake
  command: ffmpeg -i {{.src_folder}}/{{.name}}.{{.extension}} -movflags use_metadata_tags -map_metadata 0 -c:v libx265 -crf 26 -preset fast -c:a aac -b:a 128k {{.dst_folder}}/{{.name}}.mp4
  extensions:
  - 3gp
  - 3gpp
  - avi
  - flv
  - m4v
  - mkv
  - mts
  - m2ts
  - m2t
  - mp4
  - insv
  - mpg
  - mpe
  - mpeg
  - mov
  - webm
  - wmv

# Block all other formats from being uploaded.
